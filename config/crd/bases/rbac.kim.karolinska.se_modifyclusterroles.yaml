---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: modifyclusterroles.rbac.kim.karolinska.se
spec:
  group: rbac.kim.karolinska.se
  names:
    kind: ModifyClusterRole
    listKind: ModifyClusterRoleList
    plural: modifyclusterroles
    singular: modifyclusterrole
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: ModifyClusterRole is the Schema for the modifyclusterroles API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of ModifyClusterRole
            properties:
              clusterRole:
                description: Name of the source ClusterRole to copy from
                minLength: 1
                type: string
              removeRules:
                description: Rules to subtract from the source ClusterRole
                items:
                  description: RemoveRule defines a rule to remove from the source
                    ClusterRole.
                  properties:
                    apiGroups:
                      items:
                        type: string
                      minItems: 1
                      type: array
                    resources:
                      items:
                        type: string
                      minItems: 1
                      type: array
                    verbs:
                      items:
                        type: string
                      minItems: 1
                      type: array
                  required:
                  - apiGroups
                  - resources
                  - verbs
                  type: object
                minItems: 1
                type: array
            required:
            - clusterRole
            - removeRules
            type: object
          status:
            description: status defines the observed state of ModifyClusterRole
            properties:
              rulesCount:
                description: Number of rules in the generated ClusterRole
                format: int32
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
